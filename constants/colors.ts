import { theme, ThemeConfig } from "antd";

export const LIGHT_COLORS: ThemeConfig | undefined = {
  token: {},
};

export const DARK_COLORS: ThemeConfig | undefined = {
  token: {},
  algorithm: theme.darkAlgorithm,
};
